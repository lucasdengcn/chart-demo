apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chart-demo.fullname" . }}-test-connection"
  labels:
    {{- include "chart-demo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chart-demo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
